feat: 监察思考带专门标记「〔监察思考〕」+ 去掉重复打印的那一份纠正副本

用户两点：①监察的思考与执行的思考混在一起，要改成专门的监察思考；②监察注入会重复打印。

① appendTwinRecord 的 reasoning 文本统一加前缀「〔监察思考〕」（执行侧思考不动），
   折叠态首行即标记，一眼能分出是谁的；reasoning 块仍然永远在场，不回退上一轮的
   reasoning_content 硬约束修复。
② 删掉 injectCorrection（连同调用点）：纠正已经随 deny 原因（工具结果里的 `Error: [监察] …`）
   或 `_twin_note` 的工具结果到达模型，再 agent.inject 一份就是同一段文字在会话里出现两次
   （工具结果一次、监察记录一次、注入第三次）。现在只剩前两处，模型侧与用户侧都不缺信息。

诊断口径：_dev/find_bad_records.cjs --dup（复用同一套解码/遍历，不另建脚本；自带"一例命中、
一例不命中"自检，会话目录缺失/扫到 0 个/打印条数与计数不一致都会直接报错退出，解码失败单独计数）。
实测：扫 26 个会话、含监察痕迹 12 个，"注入纠正与记录同文"0 处——因为真正的重复是
"记录 vs 工具结果 vs 注入"三份同源，已按②收敛。
桩测 27/27。
