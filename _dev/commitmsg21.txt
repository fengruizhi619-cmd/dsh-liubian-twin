feat: 监察思考默认不进上下文（只留最短标记）+ 提示词要求写短

用户指定：思考过程可以不注入上下文；提示词也让监察把输出写简要些。

1. 会话记录：reasoning 块默认只写一句「〔监察思考：按设置不入上下文〕」（不能整块省——thinking 模式
   要求回传的助手消息必须带 reasoning_content，缺了会 400）；裁决那一行照旧留在会话里可见。
   要看思考全文：`~/.dsh/liubian-twin/sessions/<id>.jsonl` 一直有；要恢复思考进会话，config.json 里
   把 `twinRecordThinking` 设 true（届时按 `twinRecordChars`，默认 1200 字截断）。
   效果：以前一条记录最多上万字、且每轮请求重发；现在只占几十字。
2. criteria.json：contract 改成"第一行结论词 + 第二行起最多两句（合计 60 字以内），不复述判据、
   不罗列对照过程、不重复动作原文"；role 加第 8 条同义要求。

验证：node --check 通过；专项回归（三类贡献点 reasoning 块）通过；桩测 27/27 通过。
生效：需插件重载或 DSH 重启。
