feat: 送监输入改成 Markdown 四块（要求 / 上下文依据 / 待判别动作 / 现在开始）

用户指定：输入格式要按 md 标出哪些是上下文依据、哪些是需要判别的、哪些是要求。

buildInstruction 重写为一份 Markdown 文档：
  # 监察任务 + 一句角色定位
  ## 一、要求（你要照做的）        role + usage + ### 判据（44 条编号列表）+ ### 输出要求（结论词契约）
  ## 二、上下文依据（只是背景，不要接着做、不要调用工具）
                                  拍平后的最近记录 + **用户本人的原始指令**（引用块 «»）
  ## 三、待判别的动作（只审这一个，发生于 turn/step）   工具名 + 参数
  ## 四、现在开始                 收束语（+ 可选文本尾巴 twinPrefill）
配套改动：
- contextTextFor()：按模式产出背景文本（flatten 拍平 / isolated 单条 / full 不给）；
  flattenTranscript 顺手跳过 source.kind==='plugin' 的注入块（那是给执行侧的指令，不该混进"依据"）。
- 隔离类模式下送监只剩这一份文档（背景已写在第二节里），不再另发一条背景消息；backgroundMessages 删除。
- dump_context.cjs 补收 agent/inbox/spliced 里的人类消息（正在跑的那一轮只以 inbox 事件存在），
  所以样张里能引用到真实用户原话；同时按插件的隔离/整段两种形状生成 wire.json。
桩测 25/25（含"四块标题齐全且顺序正确""背景里不再残留特殊标识符""用户原话引用块化"）。
