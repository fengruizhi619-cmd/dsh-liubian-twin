feat: 流变·孪生（监察）插件落地

独立插件 dsh-liubian-twin：在同一会话里挂一个监察智能体，用会话自己的模型
审执行智能体的每一个动作，偏离用户指令时当场拦下并给出可直接执行的纠正。

- 两个闸门：tools/pre-execute（deny，工具不派发）+ llm/stream 包裹（扣住文本分块）
- 判据 criteria.json：44 条平铺 items（when 使用场景 + q 要求），不写来源、不用比喻与行话，
  不装载任何技能文件
- 闸门触发：链首/类型切换必审 + 距上次审查 ≥10 次动作或 ≥10 分钟 + 每回合预算 12
- 失败策略：5s × 5 次重试；工具闸门 fail-closed（notice 固定文本 + 中断回合 + deny 兜底），
  文本闸门 fail-open（原文放行 + 末尾补申明）
- 监察思考按原生渲染：追加 assistant/message（reasoning + 〔监察〕裁决），另存 jsonl
- 递归防护：监察调用不带 sessionId + twinDepth 计数 + 内部工具白名单
- 桩测 19 项全过；闸门规则在 8 个真实会话上与参照算法逐条一致（523/248/269/6/393）
