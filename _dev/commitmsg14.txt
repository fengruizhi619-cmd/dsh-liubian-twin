feat: 落盘前的最后一道闸——配对检查（pairingOk / tailIsWritable）

顺着 013f0efa 那条对话的进度（它当时正想验证"宿主收不收我们追加的监察记录"，
但把写入做在了工具窗口里，反而把会话写坏）：

- 新增 pairingOk(messages)：每条 assistant 的每个 tool-call，后面必须**紧跟**一段连续的
  工具结果把 id 全部覆盖；中间插了别的消息就判不合法（这正是那次事故的坏法）。
- 新增 tailIsWritable(agent)：落盘前检查，不合法就推迟（1.5s 重试，最多 5 次），
  到上限仍不安全就丢弃记录 —— 宁可丢记录，绝不写坏会话。
- flushPending 接入这道闸：不论排队/定时器怎么错位，都写不进去。
- 双向收益：既拦住"我们写进工具窗口"，也拦住"往一条已经坏了的会话里再写"。

桩测 26/26（新增"中间插消息 = 不合法""已坏会话不许再写""排队项保留到安全时点"三条断言）。
