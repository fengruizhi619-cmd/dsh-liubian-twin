feat: 拍平按轮次(默认10轮)取 + 背景只留用户输入与模型输出（工具调用默认丢掉）

用户两条指示：
1) 上 10 轮对话，而不是只 6 条 → twinTranscriptRounds: 10，一轮 = 一条人类发言 + 其后动作/往返；
   每轮各留末尾 twinRoundChars(1200) 字（越近越重要，预算可预期）。twinBackgroundChars 改为全局上限(0=不限)。
2) 只给"用户的输入 + 模型的输出"，工具调用可以删掉 → twinIncludeTools 默认 false，
   readableText 不再写 [调用工具 …]/[工具结果 …]；另加 twinIncludeReasoning(默认 true) 决定要不要带思考。

实测（真会话 deepseek-flash，按默认配置）：
   10 轮 × 每轮 1200 字 → 背景 12,028 字，7.9s，deny，409 字真裁决（抓住"被审动作与用户原话对不上"）。
   不限长对照：同一会话最近 10 轮原始拍平 310,104 字 → 22.3s（所以每轮上限是必要的）。
桩测 25/25（新增"默认不写工具调用/工具结果""twinIncludeTools=true 才写"两条断言）。
