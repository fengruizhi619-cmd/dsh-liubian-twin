feat: 正文回答与用户输入不设上限（只丢工具调用内容）

用户判断：拖慢速度的是工具调用内容，不是正文回答，所以正文/用户输入不该设上限。

- twinRoundChars 默认 1200 → 0（不限）；twinBackgroundChars 仍为全局上限，默认 0=不限。
- 工具调用/工具结果继续默认丢弃（twinIncludeTools=false），这是唯一的裁剪来源。

实测（真会话 deepseek-flash，10 轮、不限长、丢掉工具内容）：
   上下文 145,453 字 → 6.8s，deny，407 字真裁决（指出被审动作与用户原话对不上）。
   对照：同样 10 轮但**带上**工具内容 310,104 字 → 22.3s。
   结论与用户判断一致：慢的是工具内容，不是正文体量；丢掉之后体量涨到 14 万字，耗时反而从 22s 降到 6.8s。
桩测 25/25。
